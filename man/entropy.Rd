% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{entropy}
\alias{entropy}
\title{Entropy}
\usage{
entropy(probs)
}
\arguments{
\item{probs}{A numeric vector representing the probabilities of different outcomes. The probabilities should sum up to 1.}
}
\value{
The entropy value calculated from the input probability distribution.
}
\description{
Entropy
}
\details{
The entropy of a random variable X with probability distribution P is
calculated as: \deqn{-\sum_{i} p(x_i) \cdot \log_2(p(x_i))}{-sum(p(x_i) * log2(p(x_i)))},
where \eqn{p(x_i)}{p(x_i)} is the probability of outcome \eqn{x_i}{x_i}.
}
\examples{
# Example usage
probabilities <- c(0.2, 0.3, 0.1, 0.4)
entropy_value <- entropy(probabilities)
print(paste("Entropy:", entropy_value))
}
